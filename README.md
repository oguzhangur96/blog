# Blog

Personal blog built with [Docusaurus](https://docusaurus.io/). Deployed to GitHub Pages via GitHub Actions.

## Installation

```bash
npm install
```

## Local Development

```bash
npm start
```

Starts a local development server. Most changes are reflected live without restarting.

## Build

```bash
npm run build
```

Generates static content into the `build` directory.
